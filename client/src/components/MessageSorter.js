import React from 'react'

const MessageSorter = ({messages}) => {
    const contactNames = []
    const user = JSON.parse(localStorage.getItem('user'))

    messages.forEach(message => {
        // next task
    })

    return (
        <div>
            
        </div>
    )
}

export default MessageSorter